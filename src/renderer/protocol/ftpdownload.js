import ftp from 'ftp';
import fs from 'fs';

export default {

    sync(task, files) {
        const syncFileCount = 0;
        const insertFiles = [];
        const updateFiles = [];

        // todo

        return {
            syncFileCount,
            insertFiles,
            updateFiles
        }
    }

}
